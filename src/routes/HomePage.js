import React from 'react';

const HomePage = () => (
  <div>
    <h1>Welcome to the Form Validation Project</h1>
    <p>Select a page from the navigation menu.</p>
  </div>
);

export default HomePage;